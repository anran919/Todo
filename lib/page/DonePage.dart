import 'package:flutter/material.dart';

/// 已经完成的清单
class DonePage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    
    return new Container(
      child: new Text('已经完成的清单'),
    );
  }
}
